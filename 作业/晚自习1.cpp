#include<stdio.h>
#include<math.h>

int main(){
	/*
	int abc,a,b,c,cba;
	scanf("%d",&abc);
	abc = abs(abc);
	a = abc/100;
	b = (abc - a*100)/10;
	c = abc - a*100 - b*10;
	cba = c*100 + b*10 + a;
	printf("%d",cba);
	*///不论正负将三位整数倒序输出
	
	/*
	float rate,capital,deposit;
	int n;
	printf("please input rate and year and your capital:");
	scanf("%f%d%f",&rate,&n,&capital);
	deposit = capital*pow(1 + rate,n);
	printf("%.2f",deposit);
	*///计算定期存款本息
	
	return 0 ;
}
