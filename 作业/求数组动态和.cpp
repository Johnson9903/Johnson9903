#include<stdio.h>

int main(){
	int i = 0;
	int a;
	int m = 0;
	int sum[i];
	printf("enter sum from sum[0] to sum[i](enter y or Y to countinue):");
	for(m = getchar();m != 'y'&&m != 'Y';m = getchar()){					//输入数组（费脑） 
	sum[i] = (int)m;
	i++;
}
	a = i-1;
	int rezult[a];
	
	
	return 0;
}
